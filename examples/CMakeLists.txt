foreach(example_name echo echo_unix multithreaded_echo)
  add_executable(${example_name} ${example_name}.cpp)
  target_include_directories(${example_name} PUBLIC ../src)
  target_link_libraries (${example_name} LINK_PUBLIC uWS)
endforeach()
